import { PriceDifferencePipe } from './price-difference.pipe';

describe('PriceDifferencePipe', () => {
  it('create an instance', () => {
    const pipe = new PriceDifferencePipe();
    expect(pipe).toBeTruthy();
  });
});
